A plain javascript plugin that allows a user to do a free text search of Flickr and returns images. It also allows users to page through the results.

This plugin features:
	- A function to construct the Flickr search query from supplied parameters (including API key).
	- JS results fed to the page from an HTML template.
	- jsonFlickrApi function to handle default Flickr JSON results.